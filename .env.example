# Porta do servidor (Render define automaticamente, deixe em branco para deploy)
PORT=3000

# Origens permitidas para CORS (separadas por vírgula)
# Para deploy no Render, adicione a URL do seu GitHub Pages
ALLOWED_ORIGINS=https://viniciusvivet.github.io,http://localhost:3000,http://127.0.0.1:5500

# Email de destino (para futuro uso com Nodemailer)
DESTINATION_EMAIL=seu-email@exemplo.com
